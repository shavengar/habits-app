import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    type: "light",
    primary: {
      main: "#FFE299",
    },
    secondary: {
      main: "#575D90",
    },
    background: {
      default: "#575D90",
      paper: "#575D90",
    },
  },
});
